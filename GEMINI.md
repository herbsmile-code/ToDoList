# ToDoList Project Rules & Guidelines

이 프로젝트(ToDoList)에서 코드를 작성하거나 수정할 때 항상 준수해야 하는 핵심 규칙입니다.

---

## 1. ☁️ 클라우드 최신 상태 우선 동기화 (Cloud-First Sync)
- **단일 신뢰 원천 (Single Source of Truth)**: 클라우드 데이터 타임스탬프(`updatedAt`)가 로컬보다 최신일 경우 클라우드 상태를 우선 동기화합니다.
- **정확한 타임스탬프 기록**: 데이터 추가, 수정, 삭제 시 항상 최신 타임스탬프(`Date.now()`)를 기록하고 즉시 클라우드에 전송(`save(true)`)합니다.
- **멀티 디바이스 안전 병합**: 다른 기기에서 수정한 내용은 ID 기반 스마트 머지로 안전하게 통합합니다.

---

## 2. 🛡️ 기존 데이터 안전 보존 & 덮어쓰기 방지 (Zero Data Loss & Anti-Overwrite)
- **사용자 수정 데이터 절대 보존**: 사용자가 직접 작성하거나 수정한 내용(프로젝트, 마일스톤, 메모, 일정, 가계부, 위시 등)은 어떠한 경우에도 기본 템플릿으로 덮어써지거나 유실되지 않도록 엄격히 보호합니다.
- **1회성 시딩(One-Time Seeding) 원칙**: 기본 템플릿(인생 프로젝트 3종, 기본 폴더 등)은 최초 1회만 안전하게 시딩(`PROJ_SEED_KEY` 등)하며, 매 로드 시마다 기본값으로 강제 리셋하는 코드를 절대 작성하지 않습니다.
- **메모리 인스턴스 대입 무결성 (Zero Memory Loss)**: `loadLocalOnly()` 시 파싱된 모든 사용자 데이터 변수는 클래스 멤버 변수(`this.projects`, `this.tasks`, `this.notes` 등)에 빠짐없이 1:1 대입하여, 빈 배열(`[]`)이 로컬 스토리지에 덮어써지는 버그를 원천 차단합니다.
- **방어적 예외 처리 (Defensive Fallback)**: 네트워크 오류, 클라우드 응답 실패, E2EE 복호화 예외 발생 시에도 로컬에 저장된 기존 데이터를 안전하게 유지합니다.

---

## 3. 🧟 삭제 항목 부활 방지 (Anti-Zombie Tombstone Integrity)
- **영구 삭제 보장**: 사용자가 삭제한 항목은 톰스톤 장부(`deletedItemIds`)에 즉시 기록되며, 새로고침, 브라우저 재시작, 클라우드 동기화 시 **어떠한 경우에도 절대 다시 부활하지 않습니다**.
- **UI 렌더링 시 무단 강제 생성 금지**: `renderProject()`, `renderTasks()` 등의 화면 렌더링 함수 내부에서 데이터가 비어있다고 해서 사용자가 명시적으로 삭제한 기본 템플릿을 무단으로 강제 재생성하지 않고, 사용자의 삭제 의도를 100% 존중합니다.

---

## 4. 🚀 캐시 버스팅 & 배포 무결성 (Deployment & Cache Busting)
- **캐시 버스팅 필수 갱신**: 자바스크립트(`app.js`) 또는 스타일(`style.css`) 변경 시 `index.html` 및 `ToDoList.html`의 쿼리 파라미터(`?v=...`)를 반드시 갱신하여 브라우저 디스크 캐시로 인한 구버전 실행을 방지합니다.
- **배포 동기화 완비**: 코드 수정 완료 시 로컬 검증뿐만 아니라 `git push origin main`을 통해 GitHub Pages 배포까지 완료하여 실제 배포 사이트와의 일치를 보장합니다.

---

## 5. 📐 코드 품질 및 보안 가이드라인
- **클린 코드 & 방어적 프로그래밍**: 단일 책임 원칙 준수, 안전한 try-catch 예외 처리, 타입 및 입력값 검증.
- **보안 가드레일**: 민감 정보 소스코드 하드코딩 금지, E2EE 종단간 암호화(AES-256) 무결성 유지, XSS 방지 이스케이프 철저.

