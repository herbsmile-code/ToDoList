# ToDoList — Domain & Data Protection Rules

이 문서는 **ToDoList 프로젝트 고유의 데이터 구조, 저장소 및 동기화 무결성**을 보호하기 위한 도메인 전용 가이드라인입니다.
(에이전트 공통 행동 및 작업 원칙은 [AGENTS.md](file:///c:/Users/JY/Desktop/vibe_coding/ToDoList/AGENTS.md)를 따릅니다.)

---

## 1. 🛡️ 기존 데이터 보존 & 덮어쓰기 방지 (Zero Data Loss)
- **사용자 수정 데이터 절대 보존**: 사용자가 직접 작성하거나 수정한 프로젝트, 마일스톤, 메모, 일정, 가계부, 위시리스트 등은 어떠한 경우에도 기본 템플릿으로 덮어써지거나 유실되지 않도록 엄격히 보호합니다.
- **1회성 시딩(One-Time Seeding) 원칙**: 기본 템플릿(인생 프로젝트 3종, 기본 폴더 등)은 최초 1회만 안전하게 시딩(`PROJ_SEED_KEY` 등)하며, 매 페이지 로드 시마다 기본값으로 강제 리셋하는 코드를 절대 작성하지 않습니다.
- **메모리 인스턴스 1:1 대입 무결성**: `loadLocalOnly()` 실행 시 파싱된 모든 사용자 데이터 변수는 클래스 멤버 변수(`this.projects`, `this.tasks`, `this.notes`, `this.schedules`, `this.accountBook`, `this.wishList`)에 빠짐없이 1:1 대입하여, 파싱 실패나 예외로 인해 빈 배열(`[]`)이 로컬 스토리지에 덮어써지는 버그를 원천 차단합니다.
- **방어적 예외 처리 (Defensive Fallback)**: 네트워크 오류, 클라우드 응답 실패, E2EE 복호화 예외 발생 시에도 로컬에 저장된 기존 데이터를 안전하게 유지합니다.

---

## 2. 🧟 삭제 항목 부활 방지 (Anti-Zombie Tombstone Integrity)
- **영구 삭제 보장**: 사용자가 삭제한 항목은 톰스톤 장부(`deletedItemIds`)에 즉시 기록되며, 새로고침, 브라우저 재시작, 클라우드 동기화 시 **어떠한 경우에도 절대 다시 부활하지 않습니다**.
- **UI 렌더링 시 무단 강제 생성 금지**: `renderProject()`, `renderTasks()` 등의 화면 렌더링 함수 내부에서 데이터가 비어있다고 해서 사용자가 명시적으로 삭제한 기본 템플릿을 무단으로 강제 재생성하지 않고, 사용자의 삭제 의도를 100% 존중합니다.

---

## 3. ☁️ 클라우드 동기화 & 보안 무결성 (Cloud-First Sync & E2EE)
- **단일 신뢰 원천 (Single Source of Truth)**: 클라우드 데이터 타임스탬프(`updatedAt`)가 로컬보다 최신일 경우 클라우드 상태를 우선 동기화합니다.
- **정확한 타임스탬프 기록**: 데이터 추가, 수정, 삭제 시 항상 최신 타임스탬프(`Date.now()`)를 기록하고 즉시 클라우드에 전송(`save(true)`)합니다.
- **E2EE 종단간 암호화 유지**: 민감한 사용자 데이터는 AES-256 종단간 암호화 무결성을 항상 유지하며, 복호화 실패 시 기존 로컬 데이터를 절대 삭제하지 않습니다.

---

## 4. 🚀 캐시 버스팅 & 배포 무결성 (Cache Busting)
- **캐시 버스팅 필수 갱신**: 자바스크립트(`app.js`) 또는 스타일(`style.css`) 변경 시 `index.html` 및 `ToDoList.html`의 쿼리 파라미터(`?v=...`)를 반드시 갱신하여 브라우저 디스크 캐시로 인한 구버전 실행을 방지합니다.
- **배포 동기화 완비**: 코드 수정 완료 시 로컬 검증뿐만 아니라 `git push origin main`을 통해 GitHub Pages 배포 사이트와의 일치를 보장합니다.
