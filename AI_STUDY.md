# 🧠 AI 스터디: MCP(Model Context Protocol) 도입 시 규칙 상충 및 아키텍처 가이드

> **기록일자**: 2026-09-08  
> **주제**: 전역 규칙(Global Rules) / 프로젝트 룰 vs 외부 MCP 도구 간의 상충(Conflict) 분석 및 시니어 개발자 관점의 해결 전략

---

## 📌 1. 스터디 배경 & 핵심 질문

* **배경**: AI 에이전트에 적용된 전역 규칙, 프로젝트 룰, 스킬과 외부에서 가져온 MCP 도구의 프롬프트(Description/Instruction)가 중복되거나 서로 충돌할 때 어떤 문제가 생기는가?
* **핵심 질문**: 상충 시 AI가 어떤 지침을 우선시하는가? 환각(Hallucination)이나 코드 유실을 방지하려면 어떻게 룰과 도구를 분리/관리해야 하는가?

---

## ⚠️ 2. 대표적인 4대 상충 시나리오 (Conflict Analysis)

| 구분 | 전역/프로젝트 규칙 | 외부 MCP 도구의 지침 | 발생 가능한 문제점 (위험 요소) |
| :--- | :--- | :--- | :--- |
| **1. UI / 스타일링** | **Vanilla CSS** 원칙 준수<br>(TailwindCSS 임의 사용 금지) | 내부적으로 **TailwindCSS**<br>클래스 생성을 강제 | 두 스타일 지침이 충돌하여 클래스와 일반 CSS가 뒤섞이거나 스타일이 깨짐 |
| **2. 승인 절차 (Safety)** | **계획 수립 후 승인** 필수<br>(사전 승인 전 파일 수정 금지) | 오류 발생 시 **즉시 자동 수정/배포**<br>(자율 연쇄 실행 유도) | 승인 절차를 건너뛰고 자율적으로 코드를 덮어쓰거나, 반대로 도구 실행 거부로 멈춤 |
| **3. 파일 편집 방식** | **부분 수정(Diff)** 원칙<br>(기존 주석/독스트링 필수 보존) | 단순 **전체 덮어쓰기(Overwrite)**<br>(`write_file` 형태) | 파일 전체를 다시 쓰면서 기존 주석, 세부 로직, 예외 처리 코드가 통째로 유실됨 |
| **4. OS / 터미널 환경** | **Windows PowerShell** 환경<br>(`cd` 및 Bash 명령어 금지) | **Linux / Mac Bash** 기준<br>(`rm -rf`, `cd &&`, `export` 등) | Windows 환경에서 리눅스 명령어가 실패하여 Syntax Error 발생 및 무한 재시도 토큰 낭비 |

---

## 💡 3. 시니어 아키텍트의 핵심 해결 철학 & 실무 수칙

> 💬 **"MCP는 '도구(API)'일 뿐이며, 프로젝트의 '규칙과 의사결정(거버넌스)'을 침범해서는 안 된다."**

### 1) 상위 권한 명시 (System Rules > Tool Instructions)
* 전역 룰에 **'출력 필터링 및 변환 지침'**을 명시하여, 도구가 어떤 템플릿(Tailwind 등)을 던져주든 최종 산출물은 프로젝트 규칙(Vanilla CSS)으로 번역하도록 강제함.
* *"외부 도구의 내부 지침보다 시스템 전역 룰이 항상 최우선한다"*는 메타 룰 선언.

### 2) 권한 분리 (Read vs Write)
* **조회형(Read-only) MCP**(DB 조회, 슬랙 읽기 등): 자유롭게 자동 호출 허용.
* **수정/파괴형(Write/Delete) MCP**: 반드시 인간 개입(Human-in-the-Loop) 및 사전 계획서(`implementation_plan.md`) 승인 후 1회씩 실행하도록 제한.

### 3) 파일 I/O 및 수정 단일화 (Single Source of Truth)
* 외부 MCP에 직접 파일 생성/덮어쓰기 권한을 주지 않고, **"코드 스니펫/패치 제안"**까지만 담당하게 함.
* 실제 파일 수정은 신뢰할 수 있는 **IDE 내장 부분 수정 도구(`replace_file_content`)**에만 맡겨 주석과 기존 코드 유실 방지.

### 4) 환경 격리 및 어댑터 구성
* 단기: 전역 룰에 *"Windows PowerShell 문법으로 자동 변환하여 실행하라"*는 어댑터 룰 주입.
* 장기: 오픈소스 CLI MCP 도입 시 **WSL2(Ubuntu)** 또는 **Docker 컨테이너** 환경을 활용하여 OS 환경을 표준화.

---

## 🛠️ 4. 5단계 워크플로우(1~5번 셋업)와의 연계 포인트

* **3단계 (`/setup-skill`)**: 외부 MCP 연동 시 신뢰할 수 있는 도구만 선별 등록하고 쓰기 권한을 철저히 검수.
* **4단계 (`/verify`)**: 도구 실행 후 스타일/주석 누락, OS 종속성 문제를 자동 테스트로 검증.
* **5단계 (`/commit`)**: MCP 작업 전후로 Git 커밋/스냅샷을 남겨 유사시 즉시 롤백 가능한 안전망 확보.
