[project]
name = "todolist"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "herbsmile-code", email = "jinyoungkim9134@gmail.com" }
]
requires-python = ">=3.10"
dependencies = []

[project.scripts]
todolist = "todolist:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
