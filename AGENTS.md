# AGENTS.md — Project Rules v2

이 파일은 프로젝트에서 작업하는 AI 코딩 에이전트의 공통 행동 및 작업 규칙이다.
목표는 빠른 작업, 기존 데이터 보호, 정량적 검증, 안전한 리팩터링이다.

---

## 1. 기본 작업 원칙
- 사용자의 현재 요청을 최우선한다.
- 요청과 직접 관련된 파일/코드부터 확인하고, 단순 수정을 위해 프로젝트 전체를 반복 분석하지 않는다.
- 승인된 범위를 벗어난 대규모 변경, 데이터 구조 변경, 마이그레이션, 대규모 리팩터링은 먼저 영향과 계획을 설명하고 승인받는다.
- 이미 완료된 분석·테스트를 불필요하게 반복하지 않는다. 중단된 작업은 현재 변경분(diff)과 상태를 먼저 확인하고 미완료 부분부터 이어간다.

---

## 2. 능동적 문제 감지 (FAST REVIEW)
- 현재 요청과 직접 관련된 코드 범위에서 명백한 버그, 데이터 유실, 보안, 호환성 위험을 발견하면 사용자가 요청하지 않았더라도 짧게 알려준다.
- 단, 이를 위해 매 작업마다 프로젝트 전체를 탐색하거나 전면적인 코드 리뷰를 수행하지 않는다.
- 발견한 문제를 이유로 승인 없이 작업 범위를 자의적으로 확대하지 않는다.
- 프로젝트 전체 심층 검토는 사용자가 `/review-project`, “전체 검토”, “전체 위험 점검” 등을 명시적으로 요청한 경우에만 수행한다.

---

## 3. Clean Code
- 하나의 함수/컴포넌트는 가능한 한 하나의 명확한 책임을 가진다.
- 함수·변수·입출력의 의미를 명확하게 유지하고, 유용한 경우 타입/명확한 시그니처를 사용한다.
- 예상 가능한 오류에는 방어적 예외 처리를 사용하되, 오류를 조용히 삼켜 데이터 손상을 숨기지 않는다.
- 기존 코드, 주석, 설정, 메타데이터를 불필요하게 삭제하거나 변경하지 않는다.

---

## 4. 데이터 안전 — 최우선 (Zero Data Loss)
- 기존 사용자 데이터는 코드 수정이나 기능 추가 때문에 삭제·유실되어서는 안 된다.
- 읽기, 파싱, 복호화, 네트워크, 클라우드 오류가 발생하면 기존 데이터를 빈 값(`[]` 등)으로 간주하여 로컬 저장소에 덮어쓰지 않는다.
- 새 필드/기능 추가 시 기존 데이터와 하위 호환성을 유지하고 안전한 기본값을 사용한다.
- 저장 구조 변경이 필요하면 변경 전 영향, 호환성, 마이그레이션/롤백 방법을 먼저 설명한다.
- 저장 성공을 확인하기 전 입력 내용을 지우거나 UI에서 성공으로 조기 표시하지 않는다.

---

## 5. Cloud Sync 안전 원칙
- 클라우드와 로컬 데이터를 단순 전체 덮어쓰기로 동기화하지 않는다.
- 로컬에 아직 서버로 전송되지 않은 변경(`localSync` 등)이 있으면 그 미전송 데이터를 우선 보호한다.
- 미전송 데이터가 없을 때 `updatedAt` 등 프로젝트가 정한 버전 타임스탬프 기준으로 최신 상태를 판단한다.
- 삭제된 항목이 오래된 로컬 캐시 때문에 다시 살아나는 Zombie Item을 방지한다 (톰스톤 장부 보존).
- 업로드 성공은 실제 서버 성공 응답을 확인한 뒤 판단한다.
- 오프라인, 네트워크 오류, 서버 응답 실패, 암호화 복호화 오류 시 로컬의 기존 데이터를 임의로 비우거나 덮어쓰지 않는다.

---

## 6. Security
- API Key, Token, Password 등 민감정보를 소스코드에 절대 하드코딩하지 않는다.
- `.env`, 인증 키, 개인 백업 등이 Git에 포함되지 않도록 `.gitignore`를 확인한다.
- 사용자 입력은 필요한 범위에서 검증·이스케이프하여 XSS/악성 스크립트/잘못된 입력을 방지한다.
- 보안 개선 때문에 기존 기능이나 데이터 형식을 임의로 깨뜨리지 않는다.

---

## 7. 검증 수준 자동 조절 (정량적 기준)
모든 수정에서 전체 테스트를 실행하지 않는다. 수정된 파일 및 로직 위험도에 맞게 최소 충분 범위만 검증한다.

### 🟢 LOW (단순 UI / 스타일 / 문서)
- **대상**: `*.css`, `*.md`, 단순 HTML 마크업/문구 수정
- **검증**: 변경된 UI 요소의 렌더링 및 스타일 깨짐 유무만 확인

### 🟡 NORMAL (일반 기능 / 프론트 로직)
- **대상**: 컴포넌트 렌더링 함수(`render*`), 버튼 클릭 및 이벤트 리스너, 화면 전환 로직
- **검증**: 해당 기능의 동작과 직접 연결된 이벤트 흐름만 확인

### 🔴 HIGH (데이터 저장 / 동기화 / 보안)
- **대상**: `localStorage`, `IndexedDB`, `Firebase`, `Sync`, 암호화(Crypto), 데이터 모델 파싱 함수
- **검증**: 데이터 추가/수정/삭제 시 저장소 반영, 새로고침 후 데이터 유지, 에러 발생 시 데이터 보존 여부 철저 검증

### 🟣 FULL (전체 검증)
- **대상**: 대규모 리팩터링 완료 후, 또는 사용자가 `/verify-full` / "전체 검증"을 요청한 경우
- **검증**: 전체 기능 및 회귀 테스트 종합 수행

---

## 8. 안전한 리팩터링
- 대규모 파일 분리를 한 번에 수행하지 않는다.
- 의존성이 낮고 데이터 안전에 영향이 적은 기능부터 작은 단위로 분리한다.
- 각 단계마다 기존 동작을 확인한 뒤 다음 단계로 진행한다.
- 데이터 저장/동기화/암호화 같은 고위험 핵심 로직은 안전장치와 테스트가 확보된 뒤 분리한다.
- 대규모 리팩터링 전에는 안정된 Git 복구 지점을 반드시 확보한다.

---

## 9. Git 커밋과 버전 관리 분리
- **Git Commit (작업 복구 지점)**: 기능 단위 또는 버그 수정 단위로 안전하게 자주 수행하여 언제든 되돌릴 수 있는 복구 지점을 만든다 (`/commit`).
- **배포 버전 관리 (SemVer & 캐시버스팅)**: 커밋할 때마다 버전을 연속 상승시키지 않으며, 실제 배포 시점 또는 일일 작업 마무리 시점에만 버전 번호(`?v=...`)를 상승시킨다.
- GitHub push/배포는 사용자가 승인하거나 명확히 지시한 경우에만 수행한다.

---

## 10. 작업 완료 보고
간단한 작업은 핵심만 간결하게 보고한다. 중요한 작업은 아래 항목 위주로 요약한다:
1. 수정한 파일
2. 핵심 변경 내용
3. 수행한 테스트와 결과 (LOW/NORMAL/HIGH 수준 명시)
4. 아직 남아 있는 중요한 위험/미완료 항목

---

## 11. 표준 개발 워크플로우 (1~5번 셋업)
1. `/setup-project` — 프로젝트 환경 및 규칙 확인 (Project Rules v2)
2. `/design-api` — 데이터/UI/API 설계가 필요한 작업에서 코딩 전 사전 설계
3. `/setup-skill` — 반복 자동화에 재사용 도구가 필요한 경우 스킬 생성
4. `/verify` — 변경 위험도(LOW~HIGH)에 맞춘 자동 검증
5. `/commit` — 작업 완료 후 안정화된 복구 지점 Git 커밋
